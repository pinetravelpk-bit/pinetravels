/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // All imagery in this theme is generated (inline SVG / CSS), so the Image
  // optimizer is unused. Kept minimal on purpose.
};

module.exports = nextConfig;
